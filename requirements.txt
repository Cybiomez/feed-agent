# Фид-агент — зависимости MVP. Держим минимум.
feedparser==6.0.11        # разбор RSS/Atom-лент
requests==2.32.3          # HTTP к провайдеру модели (OpenAI-совместимый API)
# tomllib — из стандартной библиотеки (Python 3.11+), отдельного пакета не нужно
