# Фид-агент — зависимости.
feedparser==6.0.11        # разбор RSS/Atom-лент
trafilatura==2.2.0        # скачивание статьи и извлечение чистого полного текста + картинки
aiogram==3.31.0           # Telegram-бот (дайджест с кнопками, приём нажатий)
APScheduler>=3.10         # расписание прогонов внутри бота
requests==2.32.3          # HTTP (Telegram t.me/s/ и запас для провайдеров модели)
lxml>=5.0                 # разбор HTML публичной ленты Telegram (t.me/s/)
# tomllib — из стандартной библиотеки (Python 3.11+)
# Русская выжимка — через локальный claude CLI (внешних пакетов не требует)
