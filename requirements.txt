# Фид-агент — зависимости. Держим минимум.
feedparser==6.0.11        # разбор RSS/Atom-лент
trafilatura==2.2.0        # скачивание статьи и извлечение чистого полного текста + картинки
requests==2.32.3          # HTTP (запас для будущих провайдеров модели)
# tomllib — из стандартной библиотеки (Python 3.11+)
# Русская выжимка — через локальный claude CLI (внешних пакетов не требует)
