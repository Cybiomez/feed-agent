"""feed-agent — личный фильтр новостей по вкусу (модуль экосистемы MyCelium).

Пакет собирает новости из источников, оценивает их моделью против профиля вкуса,
отбирает по порогу, собирает дайджест и отдаёт его в Telegram через шов `notify`.
Точка входа — `python -m feed_agent` (см. __main__.py).
"""

__version__ = "0.1.0-dev.1"
